<% unless presenter.about.nil? or presenter.about.first == '' %>
<% presenter.about.each do |bio| %>
    <h3>About me</h3><p class="work_description"><%= iconify_auto_link(bio) %></p>
<% end %>
<% end %>

<h4>Websites</h4>
<% unless presenter.website.nil? %>
<% presenter.website.each do |website| %>
  <%= iconify_auto_link(website) %><br />
<% end %>
<% end %>

<script>
$('.work_description').each(function(){
    var length = 500;
    var details = $(this);
    var original_html = details.html();
    var original_text = details.text();
    var truncated_text = $.trim(original_text).substring(0, length).split(" ").slice(0, -1).join(" ") + " ";
    var show_more = $('<a href="#more">...more</a>');
        show_more.on('click', function(){
          details.html(original_html);
          return false;
        })
    details.html(truncated_text);
    details.append(show_more);
  });
</script>
